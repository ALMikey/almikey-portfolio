# 工作区协作规范

## 沟通与文档

- 默认使用中文与用户沟通，并使用中文编写文档和代码注释。
- 代码标识符、命令和第三方技术术语可保留英文。

## Git 提交

- 使用 Conventional Commits：`feat`、`fix`、`docs`、`refactor`、`test`、`chore`、`build`、`ci`、`perf`。
- 格式为 `type: 简短中文祈使句`，主题不超过 72 个字符。
- 破坏性变更使用 `type!:`，或在提交正文写明 `BREAKING CHANGE:`。
- 每个提交只包含一个清晰目的。

## 验证与敏感信息

- 提交前运行目标项目可用的验证命令；无法运行时，在交接说明原因和未执行项。
- 不提交密钥、令牌、构建产物、依赖目录、无关临时文件或机器专属配置。
